// import './App.css';
// import {fetchWeather} from './api'

import React, { useState } from 'react';

export const App = () => {
  return (
    <div
      style={{
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: 40,
        color: '#010101',
      }}
    >
      Weather App
    </div>
  );
};
